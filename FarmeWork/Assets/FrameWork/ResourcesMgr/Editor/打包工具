打包工具：
	


	问题：
		PackagerRedundancyCheck(检测资源重复打包)
			这个有些问题,那么被默认打进去的资源并不能检测到
				比如:
					1、材质打包,依赖的图片可能存在两份,但是检测不到
					2、图集打包,一个图集的两张图片打进不同AB包,那么图集就会存在两份,也检测不到
					这些都是默认进了AB包里面的资源,在.manifest并没有记录,所以检测不到